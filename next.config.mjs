/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['your-domain.com', 'another-domain.com'], // Replace with your actual image domains
    },
  };
  
  export default nextConfig;
  